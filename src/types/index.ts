import { Note } from '../components/NoteCard';
import { Folder, FolderColor } from '../components/FolderItem';

// Export types from components so they can be imported from a central location
export type { Note, Folder, FolderColor };